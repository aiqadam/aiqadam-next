# AI Qadam Next — Requirement Status History
#
# APPEND-ONLY. Never rewrite, reorder, or delete an existing entry — see
# docs/agents/instructions/core-directives.md "Bookkeeping Is Not Optional".
# Written by ORCH (started/blocked events) and DOC-UPDATER (done events).
#
# Entry schema:
#   - requirement_id: REQ-NNN
#     event: started|done|blocked|escalated
#     run_id: <RUN-ID>
#     at: <ISO8601-UTC, from the clock — never estimated>
#     note: <one line, optional>
#
# No roll/volume-splitting rule exists yet — this file starts empty and is expected
# to stay small for a long time at this project's current size. If it ever grows large
# enough that a full read becomes impractical, that is a trigger to add a roll rule
# (see letflow's requirement_status.index.yaml for the pattern to adapt) — do not
# silently start truncating reads of it before that decision is made explicitly.

history:
  - requirement_id: REQ-001a
    event: started
    run_id: WF02-REQ-001a
    at: "2026-08-27T04:50:00Z"
    note: "WF-01 validated (REQ-001a/REQ-001b/REQ-002) for GitHub issue #2 'Translate to UZ'; starting WF-02 implementation for REQ-001a (i18n mechanism skeleton)."
