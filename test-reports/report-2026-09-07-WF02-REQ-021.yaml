run_id: WF02-REQ-021
date: "2026-09-07T05:59:59Z"
branch: feature/WF02-REQ-021
commit: 02361a6
lint: pass
build: pass
notes: >
  npm run lint: 0 errors, 2 pre-existing warnings (apps/web/src/components/landing/
  Footer.tsx:11 and Nav.tsx:10, <img> LCP warnings, unrelated to this diff -- both
  files untouched by REQ-021's own diff).
  npm run build: apps/web (next build) compiled successfully; apps/bot (tsc -p
  tsconfig.json) clean, zero errors.
  npx vitest run --no-file-parallelism from REPO ROOT (per ISS-0016, never
  --root apps/bot), with the scratch Postgres up and migrated (docker compose up
  -d, drizzle-kit migrate) so registration.db.test.ts's DB-backed suite actually
  ran instead of self-skipping: 16 files / 178 tests passed, 0 failed. This is the
  authoritative count for this run (not carried over from any earlier step's
  report); registration.db.test.ts alone contributed 17 of those 178 tests,
  confirmed both in isolation and as part of the full repo-root run.
cases:
  - acceptance_criterion: AC1
    result: pass
    notes: >
      Existing re-run: `npx vitest run --no-file-parallelism
      apps/bot/src/domain/registration.db.test.ts -t "REQ-021 AC4"` -> 1 passed
      | 16 skipped (17), exit 0. This spec's own live-dispatch case through the
      real register:<eventId> callback handler (capacity=1, two tappers): tap 1
      (admitted) reply exactly "You're in!\nAC1 Event\n<dateTimeText>\nWhat
      happens next: you'll get a check-in code closer to the event." (unchanged
      REQ-020 shape); tap 2 (waitlisted) reply exactly "You're on the waitlist
      for:\nAC1 Event\n<dateTimeText>\nYour position: 1\nWhat happens next:
      you'll move up automatically as seats open." -- position line correctly
      placed between the date/time line and the what-next line. Read-back:
      admission='waitlisted', qr_token IS NULL for the second tapper's row.
  - acceptance_criterion: AC2a
    result: pass
    notes: >
      Existing DB re-run: -t "REQ-021 AC2" -> 3 passed | 14 skipped (17), exit 0.
      This spec's own standalone script (scratch-verify-req021-ac2.ts), 3-row
      fixture (P/Q/R, strictly increasing created_at) seeded via Drizzle.
      Before-flip positions: {P:1, Q:2, R:3} -- exact match, proving ordinary
      ranking is correct before jumping to the after-state. Flipped row P to
      admission='admitted' (not 'withdrawn', confirming design's stated either-
      value equivalence) via direct SQL. After-flip positions: {P:null, Q:1,
      R:2} -- R correctly re-derives 3->2, not just the immediately-adjacent
      row. Row Q's updated_at unchanged: qBefore.getTime() === qAfter.getTime()
      => true.
  - acceptance_criterion: AC2b
    result: pass
    notes: >
      Same script, continuing after AC2a. Tie-break (identical created_at
      instant across two rows) checked as 3 loop iterations inside the one
      script invocation, each seeding a wholly fresh chapter/event/two-user
      fixture: {lowerIdPosition:1, higherIdPosition:2} on all 3 independent
      iterations, regardless of the two rows' physical insertion/return order
      -- confirms the tie-break is id-ascending only, never insertion- or
      scan-order-dependent.
  - acceptance_criterion: AC2c
    result: pass
    notes: >
      Same script, own fixture (capacity=5 event, one admitted-not-waitlisted
      row, distinct chapter/uuids from registration.db.test.ts's committed
      defensive cases). getWaitlistPosition on an unknown id ->
      null. getWaitlistPosition on the admitted row's id (a real row, not
      waitlisted) -> null. Neither call threw, neither returned a number.
  - acceptance_criterion: AC3
    result: pass
    notes: >
      `git grep -niE "waitlist(_|-)?(position|rank)|position(_|-)?column|
      rank(_|-)?column" -- '*.ts' ':!*.test.ts'` returned 8 match lines, all
      accounted for: i18n/types.ts:193-194 (catalog key declarations),
      i18n/ru.ts:175-176 and i18n/en.ts:168-169 (the two new catalog values),
      domain/registration.ts:219,228 (getWaitlistPosition's JSDoc/declaration),
      handlers/registration.ts:5,72,102,106,116,128,129 (call sites and the
      local, never-persisted waitlistPosition variable). One further match,
      db/schema.ts:158, is a pre-existing REQ-011-era comment explicitly
      documenting that waitlist_position (among others) "have NO column
      anywhere in this file" -- prose affirming the absence of a stored field,
      not introducing one; `git diff master...HEAD --stat -- apps/bot/src/db/
      schema.ts apps/bot/drizzle` produced zero output, confirming schema.ts
      is wholly unchanged by this branch. Filed as a MINOR spec-wording note
      (see issues below) rather than treated as a FAIL, per core-directives.md's
      structural-attribution rule (route 1: the file is unmodified by this
      diff) and because the comment's content supports rather than violates
      the invariant AC3 tests for.
  - acceptance_criterion: AC4
    result: pass
    notes: >
      Existing DB re-run: -t "REQ-021 AC4" -> 1 passed | 16 skipped (17), exit
      0. This spec's own 8-registration (past capacity=1) live-dispatch case
      through the real callback handler: tap 1 reply starts with "You're
      in!"; taps 2-9 each contain "Your position: N" for N running 1,2,3,4,5,
      6,7,8 in exact tap order (read from the actual reply string, not
      assumed), zero refusal-shaped text anywhere in any of the 9 replies.
      Read-back: count(*) WHERE admission='waitlisted' = 8, matching exactly
      (no dropped/rejected registrations).
  - acceptance_criterion: AC-catalog
    result: pass
    notes: >
      `grep -n "waitlistedPositionPrefix\|waitlistedWhatNext"
      apps/bot/src/i18n/types.ts apps/bot/src/i18n/ru.ts apps/bot/src/i18n/
      en.ts` returned all 6 expected matches (2 keys x 3 files), none missing.
      en.ts's values ("Your position:", "What happens next: you'll move up
      automatically as seats open.") verbatim-match what AC1's live dispatch
      actually produced.
issues_found:
  - severity: MINOR
    title: "REQ-021.md's AC3 accounted-for-matches list omits a pre-existing schema.ts prose comment naming 'waitlist_position'"
    description: >
      docs/agents/test-specs/REQ-021.md's AC3 lists four accounted-for match
      categories for the git grep and states any match inside
      apps/bot/src/db/schema.ts is a FAIL. The literal grep also matches
      schema.ts:158, a REQ-011-era comment ("The computed-never-stored values
      (no_show, waitlist_position, seats_taken, registration_open, finished)
      have NO column anywhere in this file") that predates this branch
      entirely -- confirmed via the spec's own companion `git diff --stat`
      check returning zero output for schema.ts -- and whose content
      affirmatively documents the absence of a stored column, the opposite of
      the regression AC3 exists to catch. TEST-RUNNER treated this as a pass
      (structurally pre-existing, and semantically supportive of the
      invariant) rather than a literal FAIL, and is flagging it so a future
      re-run of REQ-021.md's AC3 can add this comment as a fifth accounted-for
      category and avoid ambiguity.
    affected_files:
      - docs/agents/test-specs/REQ-021.md
harness_teardown:
  scratch_files_deleted: true
  docker_compose_down: true
  git_status_porcelain_clean: true
